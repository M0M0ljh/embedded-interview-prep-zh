# 100 个快速嵌入式面试问题

> 这些是来自真实嵌入式面试的真实问题。
> 反复练习，直到你能在 30 秒内回答出每道题。
> 答案在`answers/01_rapid_fire_100_answers.md`

---

## A 节 — C 语言 (1–20)

1. `volatile` 有什么作用？什么时候必须使用它？
2. 一个变量可以同时是`const`和`volatile`吗？举个例子。
3. 文件作用域与函数作用域的`static`是什么？
4. 为什么`int`对于嵌入式代码来说是危险的？你应该用什么？
5. 什么是未定义行为？举3个例子。
6. `struct padding` 和 `packed` struct 有什么区别？
7. `sizeof(struct S)` — struct 与 `char` + `int` 会是 5 吗？为什么？
8. 什么是`union`？给出一个嵌入式用例。
9. for循环中的`++i`和`i++`有什么区别？
10. 什么是类型双关语？ C 中的 union 安全吗？
11. `restrict` 关键字是什么？嵌入式有什么用处？
12. 为什么在嵌入式生产固件中避免使用`malloc`？
13. 什么是内存泄漏？如何在 裸机 MCU 上检测到它？
14. 什么是栈溢出？ FreeRTOS和裸机如何检测？
15. 如何实现循环（环形）缓冲区？
16. 什么是字节序？ ARM Cortex-M 默认情况下是小端还是大端？
17. 什么是位带？它解决什么问题？
18. `uint8_t *p` 和 `const uint8_t *p` 有什么区别？
19. `__attribute__((packed))`有什么作用？它带来什么风险？
20. `memcpy` 和 `memmove` 有什么区别？

---

## B 部分 — 中断和 ISR (21–35)

21. 列出 5 件绝对不能在 ISR 内做的事情。
22. 什么是虚假中断？
23. `volatile` 在 ISR 与主通信中修复了什么？
24. 在 ARM Cortex-M4 上，哪些寄存器会在中断进入时自动保存？
25. 尾链是什么？为什么它有效？
26. 中断上下文中什么是抢占？
27. 什么是 NMI（不可屏蔽中断）？给出一个用例。
28. NVIC是什么？如何设置 Cortex-M 上的中断优先级？
29. 中断优先级和抢占优先级有什么区别？
30. 为什么必须清除ISR内部的中断标志？
31. Cortex-M4F 上的惰性 FPU 上下文保存是什么？
32. 什么是优先级反转？是什么原因造成的？
33. 对于 ISR 信号，二进制信号量与互斥锁有何不同？
34. 什么是临界区？它是如何在裸机上实现的？
35. `__disable_irq()` 和 `BASEPRI` 有什么区别？

---

## C 部分——记忆 (36–50)

36. 绘制典型 ARM Cortex-M 应用程序的内存布局。
37. 什么是`.data`部分？什么是`.bss`？它们是如何初始化的？
38. 什么是堆？什么是栈？各自生长在哪里？
39. 裸机上的栈溢出会发生什么？
40. 什么是悬空指针？
41. 什么是use-after-free？
42. `NULL` 和野生（未初始化）指针有什么区别？
43. 什么是内存碎片？为什么它在运行 long 的系统中很重要？
44. 什么是内存池？它的优点是什么？
45. `__attribute__((section(".ccm")))`有什么用？
46. STM32 上的 CCMRAM 是什么？你不能用它做什么？
47. 启动代码如何初始化`.data`和`.bss`？
48. 链接描述文件在嵌入式中的作用是什么？
49. STM32 上的 SRAM1 与 SRAM2 是什么？他们为什么分开？
50. DMA是什么？它如何避免CPU参与数据传输？

---

## D 部分 — 外设和协议 (51–70)

51. UART、SPI 和 I2C 有什么区别？
52. 什么是波特率？计算 84 MHz 时钟上 115200 波特率的 BRR。
53. USART 中的 TXE 和 TC 标志有什么区别？
54. 4 种SPI 模式是什么？哪个最常见？
55. I2C 中的时钟拉伸是什么？
56. 什么是I2C公交车锁车？你恢复得怎么样了？
57. I2C ACK/NACK 机制是什么？
58. PWM是什么？用 ARR 和 CCR 写出占空比的公式。
59. 什么是定时器的输入捕获模式？
60. 什么是ADC过采样？你什么时候使用它？
61. 什么是DMA双缓冲？画出图案。
62. 什么是RS-485？它与 RS-232 有什么不同？
63. 什么是Modbus RTU？什么功能代码读取保持寄存器？
64. CRC-16/IBM — 多项式是什么？初始值是多少？
65. 什么是CAN框架？什么是DLC？
66. CAN 仲裁如何运作？
67. CAN节点何时转到Bus-Off？
68. 什么是CANFD？最大有效负载是多少？
69. SOME/IP是什么？
70. GPIO 推挽输出 和 开漏输出 有什么区别？

---

## E 部分 — RTOS (71–82)

71. 抢占式 和 协作式 RTOS 有什么区别？
72. 什么是蜱虫？ `configTICK_RATE_HZ`控制什么？
73. `vTaskDelay(100)` 与 `vTaskDelayUntil`。有什么区别？
74. 什么是任务栈？你如何确定它的大小？
75. `uxTaskGetStackHighWaterMark()` 返回什么？
76. 什么是信号量？ FreeRTOS 中的 3 种类型是什么？
77. 为什么不能从 ISR 提供互斥锁？
78. 什么是死锁？给出一个两个任务、两个互斥锁的例子。
79. 什么是活锁？它与死锁有何不同？
80. RTOS 中的消息队列是什么？
81. `portMAX_DELAY`是什么？使用它会带来什么风险？
82. FreeRTOS 中的 heap_4.c 与 heap_1.c 是什么？

---

## F 部分 — Linux 嵌入式 (83–91)

83. 什么是 sysfs？说出 3 件你可以通过它控制的事情。
84. 如何从 Linux 用户空间应用程序切换 GPIO？
85. 什么是内核模块？如何插入/删除它？
86. 什么是设备树？它描述了什么？
87. 什么是systemd？如何编写服务单元文件？
88. 如何调试内核崩溃（糟糕）？
89. 什么是 strace？提供一个有用的 strace 命令来调试嵌入式应用程序。
90. 什么是字符设备？它与块设备有何不同？
91. 如何在Linux中设置TCP keepalive？

---

## G 部分 — 测试和调试 (92–100)

92. 什么是嵌入式单元测试？你用什么工具？
93. 什么是 MC/DC 覆盖范围？为什么 DO-178C 需要它？
94. 什么是 HIL（硬件在环）测试设置？
95. 如何在运行时找到栈溢出错误？
96. 什么是Valgrind？你可以在嵌入式目标上使用它吗？
97. 什么是 ASAN（地址消毒剂）？
98. 如何调试 ARM Cortex-M 上的硬故障？
99. 什么是 JTAG 调试器？什么是社署？它们有何不同？
100. 你部署固件，它在实验室中可以工作，但在 field 中崩溃。
列出 5 个最可能的原因以及你将如何调查每个原因。
